// This is the target class we want to test.
public class ComputeClass2 {

    int addPlusOne(int x, int y) {
        return (x + y) + 1;
    }

    int multiplyPlusOne(int x, int y) {
        return (x * y) + 1;
    }

    int subtractPlusOne(int x, int y) {
        return (x - y) + 1;
    }

    int dividePLusOne(int x, int y) {
        return (x / y) + 1;
    }
}
